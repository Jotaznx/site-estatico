version: 0.2

phases:
  install:
    commands:
      - echo "No build step for static site"
  build:
    commands:
      - echo "Build completed"
artifacts:
  files:
    - '**/*'
    - '!buildspec.yml'
    - '!.git/*'
